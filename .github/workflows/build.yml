name: Build & Test

on:
  push:
    branches: ["**"]
  workflow_dispatch:

permissions:
  contents: read

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: 1
  DOTNET_CLI_TELEMETRY_OPTOUT: 1

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
            path: ~/.nuget/packages
            key: nuget-${{ runner.os }}-${{ hashFiles('**/packages.lock.json', '**/*.csproj') }}
            restore-keys: |
              nuget-${{ runner.os }}-

      - name: Restore
        run: dotnet restore truman.sln

      - name: Build
        run: dotnet build truman.sln --configuration Release --no-restore

      - name: Test (with coverage)
        run: |
          dotnet test truman.sln \
            --configuration Release \
            --no-build \
            --logger "trx;LogFileName=test-results.trx" \
            --coverage --coverage-output-format cobertura \
            --coverage-output ./TestResults/coverage/coverage.cobertura.xml

      - name: Upload test results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            **/TestResults/*.trx
            **/TestResults/coverage/*.xml
          if-no-files-found: ignore

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: mentaldesk/truman
          files: '**/coverage.cobertura.xml,**/TestResults/coverage/coverage.cobertura.xml,**/TestResults/coverage/*.xml'
          fail_ci_if_error: true
          verbose: true

